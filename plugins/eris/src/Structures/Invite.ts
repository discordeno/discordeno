import { DiscordInvite } from "../../deps.ts";
import Client from "../Client.ts";

export class Invite {
    constructor(data: DiscordInvite, client: Client) {
        
    }
}

export default Invite;