import { DiscordMember } from "../../deps.ts";
import Client from "../Client.ts";
import Guild from "./Guild.ts";

export class Member {
    constructor(data: DiscordMember, guild: Guild, client: Client) {

    }
}

export default Member;
